# Zig
zig-out/
zig-cache/
zig-cache-rebuild/
zig-global-cache-rebuild/
.zig-cache/

# Node.js
node_modules/
npm-debug.log
yarn-error.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# Exception: QQNT.dll shim is committed (UPX packed)
!src/packaging/QQNT.dll

# Bridge bundle (generated by pack_bridge_bundle.js, embedded into exe)
src/bridge/qqnt_bridge.bundle.bin

# Original (unprotected) DLL — only the UPX-packed version is committed
src/packaging/QQNT.dll.orig

# Config (user-specific)
config.json
*.local.json

# Logs
*.log
logs/

# Runtime (QQ installation + temp files - too large / ephemeral)
runtime/

# External wrapper binary
runtime/wrapper.node

# Shim directory (runtime-generated QQNT.dll, but source is tracked)
shim/QQNT.dll

# Test coverage
coverage/
